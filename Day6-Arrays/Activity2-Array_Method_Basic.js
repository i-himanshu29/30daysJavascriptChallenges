// Task3---------------
// use the push method to add a new number to the end of the array and log the updated array
let num = [2, 4, 6, 8];
num.push(10);
console.log("new push array", num);

// Task4----------
// Use the pop method to remove the last elements from the array and log the updated array
let popMethod = [2, 4, 6, 8];
popMethod.pop();
console.log("new pop array", popMethod);

// Task5------------
// Use the shift method to remove the first element from the array and log the updated array
let shiftMethod = [2, 4, 6, 8];
shiftMethod.shift();
console.log("new shift array", shiftMethod);

// Task6-------------
// Use the unshift method to add a new number to the beginning of the array and log the updated array

let unshiftMethod = [2, 4, 6, 8];
unshiftMethod.unshift(5);
console.log("new unshift array", unshiftMethod);
