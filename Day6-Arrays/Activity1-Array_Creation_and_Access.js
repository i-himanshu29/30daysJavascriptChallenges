// Task1------
// Create an array of numbers from 1 to 5 and log the array to the console

let num = [1, 2, 3, 4, 5];
console.log("the array is :", num);

// Task2------------
// Access the first and last elements of the  array and log them to the console

let num1 = [10, 2, 3, 4, 50, 9, 300];
let i = 0;
let firstEle = num1[i];
let lastEle = num1[num1.length - 1];
console.log(`${firstEle} and ${lastEle}`);
